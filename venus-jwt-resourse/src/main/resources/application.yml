spring:
  web:
    resources:
      static-locations: classpath:/templates/,classpath:/static/
  mvc:
    view:
      prefix: /
      suffix: .html
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/venus
    username: root
    password: admin
    type: com.alibaba.druid.pool.DruidDataSource
  application:
    name: venus-jwt-resource
  cloud:
    zookeeper:
      connect-string: localhost:3317

mybatis:
  type-aliases-package: com.kayleh.domain
  mapper-locations: classpath*:mapper/*.xml
  # classpath后面加* 可以多模块检索到mapper
logging:
  level:
    com.kayleh: debug
rsa:
  key:
    pubKeyFile: D:\project\Venus\venus-jwt-common\src\main\resources\id_key_rsa.pub
server:
  port: 9003

